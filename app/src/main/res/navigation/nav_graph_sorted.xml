<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_sorted"
    app:startDestination="@id/sortedFragment">

    <fragment
        android:id="@+id/sortedFragment"
        android:name="com.example.smartfood.Fragments.SortedFragment"
        android:label="SortedFragment"
        tools:layout="@layout/fragment_sorted">
        <action
            android:id="@+id/action_sortedFragment_to_seasonalDemandFragment2"
            app:destination="@id/seasonalDemandFragment2" />
    </fragment>
    <fragment
        android:id="@+id/seasonalDemandFragment2"
        android:name="com.example.smartfood.SeasonalDemandFragment"
        android:label="SeasonalDemandFragment"
        tools:layout="@layout/fragment_sorted"/>
</navigation>